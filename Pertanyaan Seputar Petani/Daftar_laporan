<?php
include '../Login/config.php';

$query = "SELECT * FROM laporan WHERE id_pengguna = ?";
$stmt = $conn->prepare($query);
$stmt->bind_param("i", $id_pengguna);
$id_pengguna = $id
$stmt->execute();
$result = $stmt->get_result();

if ($result->num_rows > 0) {
    // Tampilkan isi laporan
    while ($row = $result->fetch_assoc()) {
        echo "ID Laporan: " . $row['id_laporan'] . "<br>";
        echo "Alasan Laporan: " . $row['alasan_laporan'] . "<br>";
        echo "Tanggal Laporan: " . $row['tanggal_laporan'] . "<br>";
        // Jika laporan terkait dengan pertanyaan, Anda bisa tambahkan kode untuk menampilkan detail pertanyaan
        // Jika laporan terkait dengan jawaban, Anda bisa tambahkan kode untuk menampilkan detail jawaban
        echo "<hr>";
    }
} else {
    echo "Tidak ada laporan yang ditemukan.";
}
$stmt->close();
$conn->close();
?>
<!DOCTYPE html>
<html>
<head>
    <title>Daftar laporan</title>
    <link rel="stylesheet" href="../Dashboard/style.css">
</head>
<body>
    <!-- Tempatkan tampilan HTML Anda di sini -->
</body>
</html>
